#ifndef FUNCTIONS_H
#define FUNCTIONS_H

namespace itandroids
{
    int max(int a, int b);

    void swap(int a, int b);

    float pow(float *a, float *b);

    void printArray(char arr[]);
}

void printArray(char arr[]);

#endif